<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis-org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.mapper.emailMapper">
	<cache></cache>
	
	
	<select id="countSellerID" parameterType="String" resultType="Integer">
		select count(SELLER_EMAIL)
		from SELLER
		<where>
			SELLER_EMAIL=#{email}
		</where>
	</select>
	
	
	<resultMap type="AuthInfo" id="authInfoMap">
		<id column="email" jdbcType="VARCHAR" property="email" />
		<result column="divide" jdbcType="VARCHAR" property="divide" />

	</resultMap>

	<select id="selectByEmailAndDivide" resultMap="authInfoMap"	parameterType="String">
		select member_email as email from member where member_email = #{member_email}
		union
		select coach_email as email from coach where coach_email = #{coach_email}
		union
		select seller_email as email from seller where seller_email = #{seller_email}
		union
		select manager_id as email from manager where manager_id = #{manager_id}
	</select>
</mapper>