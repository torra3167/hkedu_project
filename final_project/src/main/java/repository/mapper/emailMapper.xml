<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis-org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="repository.mapper.emailMapper">
	<cache></cache>
	
	
	<select id="countSellerID" parameterType="String" resultType="Integer">
		select count(SELLER_EMAIL)
		from SELLER
		<where>
			SELLER_EMAIL=#{email}
		</where>
	</select>

	<select id="selectByEmail" resultType="FindIDCommand"	parameterType="String">
		select member_email as email from member where member_email = #{email} and member_phone=#{phone}
		union
		select seller_email as email from seller where seller_email = #{email} and seller_phone=#{phone}
	</select>
</mapper>